{
  "_comment": "Copy to lathe.config.json (gitignored) and edit. An explicit env var ALWAYS overrides a value here (env > config > default). NEVER put a push token in this file \u2014 use env or the git credential helper.",
  "analyst": {
    "url": "http://127.0.0.1:8787/v1/chat/completions",
    "model": "sonnet"
  },
  "implementer": {
    "url": "http://127.0.0.1:8089/v1/chat/completions",
    "model": "openai:local"
  },
  "tries": 3,
  "checkin": {
    "remote": "origin"
  },
  "_notes": {
    "subscription": "Point analyst.url at claude_proxy.py (wraps the Claude subscription as an OpenAI endpoint, $0/token) for the thinking; keep implementer local for the bulk. That is the cost leverage.",
    "higher+higher": "For max correctness (no cost constraint), set implementer.model to 'claude' too \u2014 both roles then use the strong model.",
    "personas": "priority: name->weight multiplier on the decider's score (CE reviewers are boosted here by default - they're the strongest). mandatory: YOUR default agent(s) injected into EVERY review/planning call (1-2 names: vendored lens names or catalog personas). The decider ALSO guarantees >=1 CE reviewer in every selection regardless of config (the CE floor). Ratings (lathe agent rate) reweight the market by measured performance; buckets (lathe agent bucket) show when to invoke which."
  },
  "personas": {
    "priority": {
      "correctness-reviewer": 1.5,
      "adversarial-reviewer": 1.5
    },
    "mandatory": []
  }
}