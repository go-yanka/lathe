SEVERITY: high | WORKFLOW_REFERENCE_HARNESS.md:§3 (`assumption_logic.py:49`) | The doc asserts `"high+med"` resolves to the `med` policy because `elif 'med' in p:` fires, but it only shows the single elif line — it never shows what precedes it. `"high+med"` also contains the substring `"high"` (it starts with it). The documented severity ladder itself lists `high` *before* `med` (`off/none/advisory/high/med/all/low`), which is the natural order to write the elif chain in. If there is an earlier `elif 'high' in p:` branch (as the ladder ordering suggests), `"high+med"` would satisfy *that* branch first and short-circuit — resolving to `allowed = {'high'}` only, never reaching line 49 at all. Concrete failing scenario: operator sets `--think high`, dial stamps `LATHE_ASSUMPTION_POLICY=high+med` (`spine_core.py:17`); if branch order is high-before-med, `unconfirmed_blockers`/`blocking_assumptions` silently drop medium-severity assumption blockers that the name "high+med" and the doc both claim are caught — the assumption gate passes builds it shouldn't. | Quote the full elif chain (including the branch(es) preceding line 49, and the equivalent context around line 84) in the doc, or add a unit test asserting `resolve_policy("high+med") == {"high","med"}` and cite its line, so the claim is grounded in an executed check rather than an assumed branch order.

SEVERITY: medium | WORKFLOW_REFERENCE_HARNESS.md:§2 (re-entrancy guard) + §6b (`do`/`build` gate:0 NOTE) | Two independently-correct design decisions compose into a gate-skip. (1) The re-entrancy guard makes any nested inner `main()` call "run RAW" — i.e., phase 4 (`_phase_gates`) never executes for that nested invocation, because it never re-enters the spine at all. (2) The `do`/`build` contracts set `gate:0` specifically because "the ENGINE already runs the standing regression inside the build." That justification is only sound if the engine's internal invocation of the standing gates is a direct function call, not a re-entrant `lathe <cmd>` subprocess dispatch. If any workflow step composes `do`/`build` by shelling out to a *nested* `lathe assume`/`lathe gate` invocation (per §6a's bug-fix/enhancement/sdlc workflows, which explicitly call `assume` as an AUTO step with `gate:1` semantics), that nested call hits the re-entrancy guard and runs RAW — skipping phase 4 for it — while the outer `do`/`build` also skips phase 4 by contract. Net result: a composed run can complete green with the standing-gate suite (`qa/run_gates.py`, 10 checks) never actually executing on either the inner or outer leg. | Have §2 or §6a state explicitly whether the "gates run inside the build" claim is a direct function call (safe) or a re-entrant subprocess dispatch (unsafe under the guard), and if the latter, make `_phase_gates` fire once at the outermost spine frame regardless of the inner contract's `gate` flag.

SEVERITY: medium | WORKFLOW_REFERENCE_HARNESS.md:§3 (`lathe.py:1790-1791`, `os.environ.setdefault`) | Fill-only dial semantics ("env > profile > config > default: fill only unset") assume each `lathe` invocation starts from a clean environment. In any long-lived shell/CI job that invokes `lathe` multiple times in the same process/session, the *first* invocation's `depth_env` call permanently stamps `LATHE_TRIES`/`LATHE_SELECT_N`/`LATHE_ASSUMPTION_POLICY` into the real OS environment; every subsequent invocation in that same session inherits those values and `setdefault` refuses to overwrite them. Concrete scenario: an operator runs `lathe do --think casual` once (stamps `LATHE_ASSUMPTION_POLICY=off`), then later in the same shell runs `lathe do --think high` expecting the strict "high+med" assumption gate — the dial silently no-ops because the env var from the casual run is already set, and the "high" run executes with assumption gating fully `off`, with no error or warning surfaced. | Document (and ideally enforce) that dial-relevant env vars must be scoped per-invocation (e.g., `env -u LATHE_ASSUMPTION_POLICY lathe ...`, or have the spine snapshot/restore these vars around each run rather than relying on process-lifetime `setdefault`).

SEVERITY: low | WORKFLOW_REFERENCE_HARNESS.md:§3 (`resolve_thinking`) | The doc says flag → env → config are checked "in order," each lower/strip-normalized, falling through to `medium` if unrecognized — but doesn't specify whether a present-but-empty string (e.g., `--think=""` from a templated CI wrapper substituting an unset variable, or `LATHE_THINK=""`) is treated as "no value, keep checking the next source" or "a value was given, it's invalid, default to medium now." Worst/empty-case per this project's own bug history: an operator with `LATHE_THINK` unset but a wrapper script exporting `--think=""` by accident would, under the second interpretation, silently lock to `medium` even though a valid env or config value was set and should have taken effect. | State explicitly in §3 whether emptiness is treated as absence (falls through) or as an invalid explicit value (stops and defaults), and cite the exact line where that branch is decided.