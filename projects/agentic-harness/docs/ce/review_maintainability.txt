SEVERITY (low) | docs/WORKFLOW_REFERENCE_HARNESS.md:whole document | The doc's entire evidentiary structure is dozens of hand-pinned `file:line` citations (e.g. `lathe.py:1760-1833`, `spine_core.py:4-11`, `run_gates.py:24-33`, `assumption_logic.py:49,84`, `CLAUDE.md:9-17`) rather than symbol/function-name anchors. This is the doc-equivalent of tight coupling to implementation details: any unrelated edit to those source files (adding a blank line, reordering a dict, wrapping a comment) silently invalidates citations without changing the described behavior, and nothing in the file states which commit/revision the line numbers were pinned against. §7 itself demonstrates the failure mode — it had to hand-verify and correct three separate stale-citation errors from the prior draft (`CLAUDE.md:9-19` → `9-17`), showing this drift already happened once for this very document. | Anchor citations to stable symbols (`resolve_thinking()`, `CONTRACT_FOR`, `CHECKS`) with line numbers as a secondary aid, or state the source commit hash this doc is pinned against, so staleness is detectable without re-deriving every citation by hand.

No other findings. This file is a static analysis/reference document, not source code — none of the structural-code criteria this review targets (complexity moved vs. removed, premature abstraction, thin wrappers, type-safety holes, file-size regressions in code, module coupling) have an applicable target here; there is no control flow, no types, and no code structure to assess.