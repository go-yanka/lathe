SEVERITY: high | docs/WORKFLOW_REFERENCE_HARNESS.md:§6a "sdlc" row | The row's parenthetical list enumerates 7 commands — `clarify`,`sdlc`,`ack`,`assume`,`build`,`trace`,`review auto` — but the same cell labels the count "6 AUTO". Concrete check: count the comma-separated items (1 clarify, 2 sdlc, 3 ack, 4 assume, 5 build, 6 trace, 7 review auto) = 7, not 6. Every other row in the same table is internally consistent (code-review 1=1, bug-fix 5=5, enhancement 3+1=4, doc-review 1=1, new-project 2=2), so this is an isolated miscount, not a formatting convention. A reader trusting the "6 AUTO" summary (e.g. to reconcile against `lathe flow sdlc` output) will expect one fewer automated step than actually exists and may conclude the table is wrong about which step is missing, or miscount total steps (6+1+4=11 vs actual 7+1+4=12). | Recount the sdlc steps against `workflows.py`/`WORKFLOW_REFERENCE_STEPS.md` and correct the label to "7 AUTO" (or remove one of the two listed items if one is genuinely GATE/YOU, not AUTO).

SEVERITY: medium | docs/WORKFLOW_REFERENCE_HARNESS.md:§3 ("On the `high+med` token") and §7 item 4 | The doc asserts `'high+med'` "resolves to exactly the documented `med` policy" because `blocking_assumptions` hits `elif 'med' in p:` at `assumption_logic.py:49`. But the string `'high+med'` also contains the substring `'high'` — a fact the doc's own caveat conspicuously omits (it only rules out `'all'` and `'low'` as substrings, never addresses `'high'`). Since `off/none/advisory/high/med/all/low` are all listed as recognized tokens in `GATES_REFERENCE.md` §1.8, an `elif 'high' in p:` branch almost certainly exists in `assumption_logic.py`, and if it appears before line 49 in the if/elif chain (plausible under any natural off→advisory→all→high→med→low ordering), `'high+med'` would match on `'high'` first and never reach the quoted `'med'` branch — yielding `allowed={'high'}` only, not `{'high','med'}` as claimed. The doc quotes only the isolated `elif 'med' in p:` line, not the preceding branches, so the "nonetheless correct" conclusion is asserted, not traced. If it's actually wrong, the `high` thinking-dial tier silently runs a narrower assumption-blocking policy than the whole rigor-gate story claims. | Quote the full if/elif chain in `assumption_logic.py` (or the equivalent at line 84) including any `'high'`-testing branch that precedes line 49, and confirm empirically (e.g., a one-line trace/test of `resolve_thinking` → `depth_env('high')` → policy dispatch) rather than asserting correctness from a single isolated line.

SEVERITY: low | docs/WORKFLOW_REFERENCE_HARNESS.md:§2 (phase table rows "1 — front-end" / "2 — select") vs. the document's opening claim | The header states "Every factual claim is grounded at `file:line` against the pinned sources; nothing is asserted from task text," yet the Phase-1 and Phase-2 rows cite only "contract keys, `workflows.py` `CONTRACT_FOR`; dispatched in-work" with no line numbers, unlike every other row in the same table (which all carry precise `lathe.py:NNNN-NNNN` cites). This is a self-consistency gap: the document's stated verification bar is violated by two of its own six phase rows. | Either add concrete line cites for the front-end/select dispatch in `lathe.py`/`workflows.py`, or soften the opening claim to acknowledge these two rows are grounded at file (not line) granularity.